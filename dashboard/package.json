{
  "name": "can-bus-dashboard",
  "version": "1.0.0",
  "description": "Live CAN bus traffic viewer",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
